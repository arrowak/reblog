<div class="page-header">
  <h1>Activity Updates</h1>
</div>
<% @activities.each do |activity| %>
  <% if current_user.follows?(activity.owner) %>
    <%= render_activity(activity) %>
  <% end %>
<% end %>
